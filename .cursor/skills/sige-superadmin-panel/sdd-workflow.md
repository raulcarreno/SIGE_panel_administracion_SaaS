# SDD workflow

1. **Spec** — OpenAPI (`docs/panel-api.openapi.yaml`), reference `docs/control-api.openapi.yaml`
2. **Review** — Align with `folder-tree.md` and `architecture-layers.md`
3. **Tests** — TDD (see tdd-workflow.md)
4. **Implement** — Minimal diff
5. **Docs** — Update specs only if contract changed
